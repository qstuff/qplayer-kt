<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="org.qstuff.qplayer" >

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <application
        android:name=".QDeqApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QPlayer"
        tools:ignore="GoogleAppIndexingWarning">

        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_analytics_collection_deactivated"
            android:value="true" />
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />

        <activity
            android:name=".player.PlayerActivity"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".settings.WebViewActivity"
            android:theme="@style/Theme.QPlayer"
            android:screenOrientation="sensorPortrait">

        </activity>

        <activity
            android:name=".settings.SettingsActivity"
            android:theme="@style/Theme.QPlayer"
            android:screenOrientation="sensorPortrait">
        </activity>

        <service
            android:name=".player.mediaservice.QMediaPlayerService"
            android:exported="false"
            android:enabled="true">
        </service>

        <receiver
            android:name=".player.mediaservice.QMediaPlayerService$NotificationBroadcastReceiver"
            android:exported="true" >
            <intent-filter>
                <action android:name="org.qstuff.qplayer.player.service.ACTION_PLAYER_TOGGLED" />
                <action android:name="org.qstuff.qplayer.player.service.ACTION_NOTIFICATION_CLICKED" />
                <action android:name="org.qstuff.qplayer.player.service.ACTION_NOTIFICATION_DISMISSED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>